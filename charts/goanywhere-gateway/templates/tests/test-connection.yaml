apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "goanywhere-gateway.fullname" . }}-test-connection"
  labels:
    {{- include "goanywhere-gateway.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['echo', 'insert tests here']
      # command: ['wget']
      # args: ['{{ include "goanywhere-gateway.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
